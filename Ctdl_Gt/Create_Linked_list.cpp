#include "SLL.h"
#include "DLL.h"
#include <iostream>
using namespace std;
int ti()
{
}
int main()
{
    /*SinglyLinkedList*/
    // SllNode a;
    // a.addHead(10);

    // // cout << a.head->data;
    // a.addHead(20);
    // // cout << a.head->data;
    // a.addHead(30);
    // cout << a.head->link->link->data;

    // DoublyLinkedList a;
    // a.append(10);
    // // a.append(20);
    // cout << a.head->data;

    cout << ti();
    return 0;
}